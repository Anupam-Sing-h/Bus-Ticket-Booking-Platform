const API_BASE_URL = "http://localhost:8000" || "https://bus-ticket-booking-platform.onrender.com";
//to make the fronted work on the local system without running the backend 
// on the system you need to replace every "http://localhost:8000" with ${API_BASE_URL}
// otherwise you need to learn both backend & fronted on the local system as Normal projects.
export default API_BASE_URL;
